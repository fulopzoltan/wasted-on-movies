export type ButtonColors = keyof typeof theme.button;
export const theme = {
    button: {
        PRIMARY: '#ff4500',
        DEFAULT: '#FEFEFE'
    },
    text: {
        main: '#ff4500',
        white: '#FEFEFE'
    },
    background: {
        main: '#000'
    }
};
